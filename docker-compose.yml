version: '3.8'

services:
  surrealdb:
    image: surrealdb/surrealdb:v1.5.0
    container_name: researchgraph-db
    command: start --auth --user root --pass root memory
    ports:
      - "18000:8000"
    volumes:
      - surrealdb_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  surrealdb_data:
